{
  "actions": 0,
  "capabilities": 2560,
  "classType": [
    "source"
  ],
  "description": "A user interface component that provides a web-based dropper experience. It \ncreates its own HTTP endpoint, configured by host and port, to serve a simple \ndropper UI. Users can interact with the interface to submit questions, which \nare routed through the attached pipeline for processing. Designed for easy \nintegration, it enables the addition of conversational capabilities to any \npipeline workflow.",
  "documentation": "https://bit.ly/4nw9mRG",
  "icon": "dropper.svg",
  "lanes": {
    "_source": [
      "tags"
    ]
  },
  "Pipe": {
    "schema": {
      "properties": {
        "hideForm": {
          "default": true,
          "type": "boolean"
        },
        "mode": {
          "default": "Source",
          "type": "string"
        },
        "parameters": {
          "type": "object"
        },
        "type": {
          "default": "dropper",
          "type": "string"
        }
      },
      "required": [
        "hideForm",
        "type",
        "mode"
      ],
      "title": "Dropper",
      "type": "object"
    },
    "ui": {
      "hideForm": {
        "ui:options": {
          "label": false
        },
        "ui:widget": "hidden"
      },
      "mode": {
        "ui:options": {
          "label": false
        },
        "ui:widget": "hidden"
      },
      "parameters": {
        "ui:options": {
          "label": false
        }
      },
      "type": {
        "ui:options": {
          "label": false
        },
        "ui:widget": "hidden"
      },
      "ui:order": [
        "hideForm",
        "type",
        "mode",
        "parameters"
      ]
    }
  },
  "plans": null,
  "prefix": "Dropper",
  "protocol": "dropper://",
  "tile": [],
  "title": "Dropper"
}