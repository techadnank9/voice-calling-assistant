{
  "actions": 0,
  "capabilities": 2560,
  "classType": [
    "source"
  ],
  "description": "A source component that listens for incoming HTTP requests and accepts \nuploaded documents or data from external systems or processes. It enables \nseamless integration for document ingestion, allowing external systems to \ntrigger the component and send files for further processing. Configurable \nwith endpoint and authentication settings, it ensures secure and reliable \ndocument transfer for pipeline processing. Ideal for receiving documents, \nimages, or other files from external platforms via HTTP-based communication.",
  "documentation": "https://bit.ly/3XaDTK4",
  "icon": "webhook.svg",
  "lanes": {
    "_source": [
      "tags",
      "text",
      "audio",
      "video",
      "image"
    ]
  },
  "Pipe": {
    "schema": {
      "properties": {
        "hideForm": {
          "default": true,
          "type": "boolean"
        },
        "mode": {
          "default": "Source",
          "type": "string"
        },
        "parameters": {
          "object": "parameters",
          "type": "object"
        },
        "type": {
          "default": "webhook",
          "type": "string"
        }
      },
      "required": [
        "hideForm",
        "type",
        "mode"
      ],
      "title": "Web Hook",
      "type": "object"
    },
    "ui": {
      "hideForm": {
        "ui:options": {
          "label": false
        },
        "ui:widget": "hidden"
      },
      "mode": {
        "ui:options": {
          "label": false
        },
        "ui:widget": "hidden"
      },
      "parameters": {
        "ui:options": {
          "label": false,
          "nonNestedDisplay": true
        }
      },
      "type": {
        "ui:options": {
          "label": false
        },
        "ui:widget": "hidden"
      },
      "ui:order": [
        "hideForm",
        "type",
        "mode",
        "parameters"
      ]
    }
  },
  "plans": null,
  "prefix": "WebHook",
  "protocol": "webhook://",
  "tile": [],
  "title": "Web Hook"
}